<%- layout('layouts/main.ejs') %>

<h1>Historial</h1>

<table class="table">
    <thead>
        <tr>
            <th>N°</th>
            <th>ubicacion</th>
            <th>Fecha</th>
            <th>Ruta</th>
            <th>Estado</th>
            <th>Generar Boleto</th>
        </tr>
    </thead>
    <tbody>
        <% for(var i=history.length -1 ; i >= 0 ; i--) { %>
            <tr>
                <td><%= i%></td>
                <td><%= history[i].ubicacion %></td>
                <td><%= history[i].date %></td>
                <td><%= history[i].ruta %></td>
                <td><%= history[i].used %></td>
                <td><button>Generar Ticket</button><td>
            </tr>
        <% } %>
    </tbody>
</table>